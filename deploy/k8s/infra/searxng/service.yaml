apiVersion: v1
kind: Service
metadata:
  name: searxng-service
  namespace: deepforge
  labels:
    app: searxng
spec:
  selector:
    app: searxng
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP